<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 166 40" width="166" height="40">
  <!-- Deployment branding asset. The header paints it through a CSS mask, so
       only the shapes reach the page and the mark takes the theme's own
       colour; these fills are what the file looks like opened on its own.
       Websafe serif stack — no font embedding. -->
  <text x="0" y="23" font-family="Georgia, 'Times New Roman', serif" font-weight="700" font-size="23" fill="#6f4e37">Coffee Kontor</text>
  <text x="1" y="36" font-family="Georgia, 'Times New Roman', serif" font-size="9" letter-spacing="2.2" fill="#8b6f52">ROASTERY &amp; WHOLESALE</text>
</svg>
