# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular

# Generated by drizzle-kit — keeps its own formatting
/apps/api/src/db/migrations

# Rendered exactly as the mailer sends them (tools/generate-mail-previews.mjs)
/docs/mail

# ID-keyed tables, hand-aligned to a fixed 12-char ID column.
# Prettier pads every cell to the widest row, so one long requirement rewrites
# the whole table and the diff stops showing what actually changed.
/docs/requirements.md
/docs/roadmap.md
